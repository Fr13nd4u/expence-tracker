import { FC } from "react";
import HeaderTitle from "../components/HeaderTitle";

const Home: FC = () => {
  return (
    <main>
      <HeaderTitle title="Home" />
    </main>
  );
};

export default Home;
